# negtest bogus commands

以下每行都是伪造的命令路径, 用来探测 skill-command-integrity 的检出边界:

`dws bogus-negtest-top`

`dws auth bogus-negtest-sub`

`dws agoal bogus-negtest-sub`

`dws config bogus-negtest-sub`

`dws skill bogus-negtest-sub`
